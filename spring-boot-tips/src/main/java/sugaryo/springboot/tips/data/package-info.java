/**
 * データパッケージ.
 * 
 * <p>
 * <b>■パッケージ概要：</b><br>
 * データパッケージには、一切のロジックを持たない単なるデータの入れ物を定義する。<br>
 * 例えば、単なる {@code JavaBeans(POJO)} や {@code ValueObject(VO)}、内外の {@code REST API} で使用する {@code JSON Class} など。<br>
 * 特性や用途に応じて {@code beans, values, entity, json} にフォルダ分けをする。
 * </p>
 * 
 * @author sugaryo
 */
package sugaryo.springboot.tips.data;